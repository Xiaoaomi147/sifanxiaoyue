import { createAction } from 'redux-actions';

export const ASL_LEFT = 'ASL_LEFT';
export const aslleft = createAction(ASL_LEFT);

export const ASL_ELLIPSIS = 'ASL_ELLIPSIS';
export const asl_ellipsis = createAction(ASL_ELLIPSIS);